#!/bin/bash

current_dir="$(dirname $0)"

if test -e "${current_dir}/script/$1/$1"; then
    path="${current_dir}/script/$1/$1"
    if test -z "$2"; then
        ./"$path"
    else
        ./"$path" "$2"
    fi
else
    echo "The script does not found!"
fi
